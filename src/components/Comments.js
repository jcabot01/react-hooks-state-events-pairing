import React from "react";

function Comments() {
  //<button that can toggle between hide = no comments viewable, to show = all comments visiable.  onClick toggle function isOn ? !==isOn
  //pass in the comments fom data file via props.
  //button needs className
  //comments header <h2 bold
  //username <h3 bold
  //comments text <h4

  return (

  );
}

export default Comments